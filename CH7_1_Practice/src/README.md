```
[7장 실습]

// 이번 실습에서 다양한 연산자를 구현할 수 있는지를 확인함.

/******************************************************************************
 * 코드 추가 1
 ******************************************************************************/
// 1) 메뉴 [File]->[New]->[C/C++ Project] 한 후 새로운 프로젝트 CH7을 만들어라.
// 2) 메뉴 [File]->[New]->[Source Folder] 한 후 폴더 이름으로 src를 지정하여 생성하라.
// 3) 메뉴 [File]->[New]->[Source File]   한 후 폴더 이름으로는 위에서 지정한 CH7/src로 
//    설정하고 파일 이름으로 ch7.cpp를 지정하여 소스파일을 만들어라.
// 4) 아래 코드는 Person 클래스를 활용하는 프로그램의 윤곽이다.
//    먼저 아래 코드([문제 1] 앞까지)를 마킹하여 복사한 후 소스 파일에 삽입하라.
//-----------------------------------------------------------------------------

#include 
#include 
using namespace std;

/******************************************************************************
 * Person class
 ******************************************************************************/
class Person {
    string *name;  // 사람 이름
    int id;        // 고유한 ID 번호
    int hours;     // 일한 시간

public:
    Person(const string& name={}, int id=0, int hour=0); // 생성자
    ~Person();                                           // 소멸자
    void print(ostream& out) const;
    friend ostream& operator << (ostream& out, const Person &p);
};

Person::Person(const string& name, int id, int hours) {
    this->id = id;
    this->name = new string(name);
    this->hours = hours;
}

Person::~Person() {
    delete name;
}

void Person::print(ostream& out) const {
    out << "name(" << *name << ") ID(" << id << ") hours(" << hours << ")";
}

ostream& operator << (ostream& out, const Person &p) {
    p.print(out);
    return out;
}

/******************************************************************************
 * menu_switch() 함수: 선택된 메인 메뉴 항목을 실행함
 ******************************************************************************/
string menuStr =
    "---------------------- Operator ---------------------\n"
    "  0.exit  1. cout <<  2. +=  3. ()  4. =  5. +(int)  \n"
    "  6. +(int,Person)  7. ==  8. ++p  9. p++  10. p <<  \n"
    "-----------------------------------------------------\n"
    "menu item? ";

void menu_switch(int menu)
{
    Person p1("HongGD",    1, 10);
    Person p2("anonymous", 2);

    switch (menu) {
    case 1:
        cout << "p1: " << p1 << endl;
        break;
    }
    cout << endl;
}
    

/******************************************************************************
 * main() 함수
 ******************************************************************************/
int main()
{
    while (true) {
        int menu;
        cout << menuStr;
        cin >> menu;
        if (menu == 0) break;
        menu_switch(menu);
    }
    cout << "\nGood bye!!" << endl;
}


/******************************************************************************
 * [문제 1] 출력 연산자 cout << Person
 ******************************************************************************/
// [문제 1 실행 결과]와 같이 출력되도록 Person의 출력 연산자 << 를 구현하라. [교재 11장을 참조하라.]
// 이 함수 구현 시 Person 객체를 출력할 때 각 멤버를 직접 출력하지 말고
// 기존 Person::print() 함수를 호출하라.
// 내용이 어려워 코드를 이미 삽하여 두었음; 그냥 실행하면 될 것임

===============================================================================
== [문제 1] 실행 결과
=============================================================================== 
---------------------- Operator ---------------------
  0.exit  1. cout <<  2. +=  3. ()  4. =  5. +(int)  
  6. +(int,Person)  7. ==  8. ++p  9. p++  10. p <<  
-----------------------------------------------------
menu item? 1
p1: name(HongGD) ID(1) hours(10)


/******************************************************************************
 * [문제 2] += 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 2:
        cout << "p1            : " << p1 << endl;
        cout << "(p1 += 1) += 2: " << ((p1 += 1) += 2) << endl;
        cout << "p1            : " << p1 << endl;
        break;

// [문제 2 실행 결과]와 같이 출력되도록 Person의 연산자 += 를 구현하라.
// 이 연산자는 매개변수로 넘어온 일한 시간 hours를 Person의 멤버 hours에 더해 준다.

===============================================================================
== [문제 2] 실행 결과
=============================================================================== 
...
menu item? 2
p1            : name(HongGD) ID(1) hours(10)
(p1 += 1) += 2: name(HongGD) ID(1) hours(13)
p1            : name(HongGD) ID(1) hours(13)


/******************************************************************************
 * [문제 3] () 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 3:
        cout << "p1  : " << p1   << endl;
        cout << "p1(): " << p1() << endl;
        break;

// [문제 3 실행 결과]와 같이 출력되도록 Person의 연산자 () 를 구현하라.
// 이 연산자는 일한 시간에 대한 임금(pay)을 계산하여 반환한다. 단위 시간당 임금은 8600원이다.

===============================================================================
== [문제 3] 실행 결과
=============================================================================== 
...
menu item? 3
p1  : name(HongGD) ID(1) hours(10)
p1(): 86000


/******************************************************************************
 * [문제 4] = 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 4:
        cout << "p1     : " << p1 << endl;
        cout << "p2 = p1: " << (p2 = p1) << endl;
        cout << "p2     : " << p2 << endl;
        break;

// [문제 4 실행 결과]와 같이 출력되도록 Person의 연산자 = 을 구현하라.
// 주의: Person에 name과 같이 포인터 변수가 있을 경우에는 반드시 = 연산자, 복사생성자, 소멸자를 
//     구현해야 한다. 여기서 복사생성자는 사용하지 않기 때문에 복사생성자는 만들지 않아도 된다.
//     만약 포인터 멤버 변수가 없다면 = 연산자는 없어도 된다.
// Person의 멤버 name이 string * 임에 주의하라.
// 참고로 string a, b; 일 경우 a = b;가 가능하다. 
//      string *a, *b가 포인터 변수라면 *a = *b가 가능하다.

===============================================================================
== [문제 4] 실행 결과
=============================================================================== 
...
menu item? 4
p1     : name(HongGD) ID(1) hours(10)
p2 = p1: name(HongGD) ID(1) hours(10)
p2     : name(HongGD) ID(1) hours(10)


/******************************************************************************
 * [문제 5] (Person + 정수) 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 5:
        cout << "p1        : " << p1 << endl;
        cout << "p1 + 3 + 4: " << p1 + 3 + 4 << endl;
        cout << "p1        : " << p1 << endl;
        break;

// [문제 5 실행 결과]와 같이 출력되도록 Person의 연산자 + 를 구현하라.
// 이 연산자는 새로운 객체 tmp를 선언(생성)하고, 
// 현 객체를 대입(먼저 구현한 대입 연산자 = 활용)하여 복사해야 한다.
// (Person tmp = *this; 형식으로 묵시적 복사생성자를 이용하여 초기화하면 안된다.
//  이유: 여기서는 복사생성자가 없기 때문에 디폴터 복사생성자를 사용하는데 이는 포인터 값을 그대로 복사함)
// 연산자의 매개변수로 넘어온 hours를 tmp 객체의 hours 멤버에 더한다.

===============================================================================
== [문제 5] 실행 결과
=============================================================================== 
...
menu item? 5
p1        : name(HongGD) ID(1) hours(10)
p1 + 3 + 4: name(HongGD) ID(1) hours(17)
p1        : name(HongGD) ID(1) hours(10)


/******************************************************************************
 * [문제 6] (정수 + Person) 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 6:
        cout << "p1          : " << p1 << endl;
        cout << "5 + (2 + p1): " << 5 + (2 + p1) << endl;
        cout << "p1          : " << p1 << endl;
        break;

// [문제 6 실행 결과]와 같이 출력되도록 Person의 연산자 + 를 구현하라.
// 이 연산자는 새로운 객체 tmp를 선언(생성)하고, 
// 매개변수로 넘어온 객체를 대입(먼저 구현한 = 연산자 활용)하여 복사해야 한다.
// 매개변수로 넘어온 hours를 tmp 객체의 hours 멤버에 더한다.

===============================================================================
== [문제 6] 실행 결과
=============================================================================== 
...
menu item? 6
p1          : name(HongGD) ID(1) hours(10)
5 + (2 + p1): name(HongGD) ID(1) hours(17)
p1          : name(HongGD) ID(1) hours(10)


/******************************************************************************
 * [문제 7] == 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 7:
        TODO: boolean 값이 true or false로 출력되게 설정하라.
        cout << "p1      : " << p1 << endl;
        cout << "p2      : " << p2 << endl;
        cout << "p1 == p2: " << (p1 == p2) << endl;
        cout << "(p2 = p1) == p1: " << ((p2=p1) == p1) << endl;
        cout << "p2      : " << p2 << endl;
        break;

// [문제 7 실행 결과]와 같이 출력되도록 Person의 연산자 == 를 구현하라.
// 이 연산자는 매개변수의 객체와 현 객체의 id가 같고, 이름도 같으면 true를 반환하고
// 그렇지 않으면 false를 반환한다.

===============================================================================
== [문제 7] 실행 결과
=============================================================================== 
...
menu item? 7
p1      : name(HongGD) ID(1) hours(10)
p2      : name(anonymous) ID(2) hours(0)
p1 == p2: false
(p2 = p1) == p1: true
p2      : name(HongGD) ID(1) hours(10)


/******************************************************************************
 * [문제 8] ++Person 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 8:
        cout << "p1  : " << p1   << endl;
        cout << "++p1: " << ++p1 << endl;
        cout << "p1  : " << p1   << endl;
        break;

// [문제 8 실행 결과]와 같이 출력되도록 Person의 전위 연산자 ++ 를 구현하라.
// 이 연산자는 현 객체의 hours 멤버를 1 증가시킨다.

===============================================================================
== [문제 8] 실행 결과
=============================================================================== 
...
menu item? 8
p1  : name(HongGD) ID(1) hours(10)
++p1: name(HongGD) ID(1) hours(11)
p1  : name(HongGD) ID(1) hours(11)


/******************************************************************************
 * [문제 9] Person++ 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 9:
        cout << "p1  : " << p1   << endl;
        cout << "p1++: " << p1++ << endl;
        cout << "p1  : " << p1   << endl;
        break;

// [문제 9 실행 결과]와 같이 출력되도록 Person의 후위 연산자 ++ 를 구현하라.
// 이 연산자는 새로운 객체 tmp를 선언(생성)하고, 
// 매개변수로 넘어온 객체를 대입(먼저 구현한 = 연산자 활용)하여 복사해야 한다.
// this 객체의 hours 멤버에 1을 더한다.
// 주의: Person은 포인터 멤버를 가지고 있기 때문에 교재처럼 복사생성자를 이용하여 초기화하면 안된다.
//      위 [문제 5] 참고

===============================================================================
== [문제 9] 실행 결과
=============================================================================== 
...
menu item? 9
p1  : name(HongGD) ID(1) hours(10)
p1++: name(HongGD) ID(1) hours(10)
p1  : name(HongGD) ID(1) hours(11)


/******************************************************************************
 * [문제 10] (Person << 정수) 연산자 구현
 ******************************************************************************/
// menu_switch(menu) 함수의 switch 문장에 아래 case 문장을 추가하라.

    case 10:
        cout << "p1      : " << p1 << endl;
        cout << "p1 << 10: " << (p1 << 10) << endl;
        cout << "p1      : " << p1 << endl;
        break;

// [문제 10 실행 결과]와 같이 출력되도록 Person의 연산자 << 를 구현하라.
// 이 연산자는 이미 구현한 += 연산자와 동일한 기능을 수행하므로 += 연산자를 활용하여 구현하라.

===============================================================================
== [문제 10] 실행 결과
=============================================================================== 
...
menu item? 10
p1      : name(HongGD) ID(1) hours(10)
p1 << 10: name(HongGD) ID(1) hours(20)
p1      : name(HongGD) ID(1) hours(20)

(끝)
```