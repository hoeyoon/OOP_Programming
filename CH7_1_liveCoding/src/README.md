```
//[객체지향프로그래밍 2024 2차 라이브코딩 7장 문제]

/******************************************************************/
// 1. 아래 코드를 복사하여 소스 파일에 삽입하라.
// 2. #include <   > 사이의 공백을 제거하라.
// 3. 아래 프로그램 실행결과와 [문제]를 참고하여 필요한 연산자를 구현하라.
/******************************************************************/

#include < iostream>
#include < string>
using namespace std;

/******************************************************************************
 * Class Person
 ******************************************************************************/
class Person {
    string name;    // 이름
    string address; // 주소
public:
    Person(string name = "", string address = "") { set(name, address); }

    void set(const string& name, const string& address) {
        this->name = name;
        this->address = address;
    }
    void show() const {
        cout << name << ": " << address << endl;
    }
    char operator [] (int idx) { return address[idx]; }
};

// 여기에 필요한 연산자를 구현하시오.


//------------------------------------------------------
// 경고: 아래 함수들은 주석을 제거하는 것 외는 수정하지 마시오.
//      만약 본인의 임의대로 아래 함수를 수정할 경우 0점 처리함.
//------------------------------------------------------

int main()
{
    int menu;
    string name1("p1"), address1("seoul"), name2("p2"), address2("XYZ");

    while (true) {
        cout << "menu item? ";
        cin >> menu;
        if (menu == 0) break;

        Person p1(name1, address1), p2(name2, address2);
        p1.show();
        p2.show();
        cout << "+++++++++" << endl;
        if (menu == 1) {
            //if (!p1) p1 = p2;
            p1.show();
            Person p3;
            //if (!p3) p1 = p2;
        }
        else if (menu == 2) {
            //(p2[0] + p1).show();
            //(p2[0] + (p2[1] + p1)).show();
            //(p2[0] + (p2[1] + (p2[2] + p1))).show();
        }
        else if (menu == 3) {
            //(p1 << p2[0]).show();
            //(p1 << p2[1]).show();
            //(p1 << p2[2]).show();
        }
        else if (menu == 4) {
            //(p1--).show();
            p1.show();
            cout << "***" << endl;
            //(p1--).show();
        }
        else if (menu == 5) {
            cin.ignore();
            cout << "name? ";    getline(cin, name1);
            cout << "address? "; getline(cin, address1);
            p1.set(name1, address1);
        }
        p1.show();
        cout << endl;
    }
    cout << "\nGood bye!!" << endl;
}


===============================================================================
== 위 프로그램은 정상적으로 컴파일될 것이다. 실행 결과는 다음과 같다.
=============================================================================== 
menu item? 1        // 메뉴 항목 번호인 1을 입력하라.
p1: seoul
p2: XYZ
+++++++++
p1: seoul
p1: seoul

menu item? 0        // 종료 시 메뉴 항목 번호인 0을 입력하라.

Good bye!!


/******************************************************************************
 * [문제 1] 
 ******************************************************************************/
// main() 함수 내의 1번 메뉴항목의 주석을 제거하라. 
// 아래 프로그램 실행결과를 참고하여 ! 연산자를 구현하라. 이 연산자는 
// 이름과 주소가 모두 빈 문자열 "" 일 경우 true, 그렇지 않은 경우 false를 반환한다.
===============================================================================
menu item? 1
p1: seoul
p2: XYZ
+++++++++
p1: seoul
p2: XYZ

menu item?  // 추후 생략

/******************************************************************************
 * [문제 2] 
 ******************************************************************************/
// main() 함수 내의 2번 메뉴항목의 주석을 제거하라. 
// 아래 실행 결과처럼 출력될 수 있도록 필요한 연산자를 구현하라.
// name 멤버의 경우 c + ("+" + p.name)로 설정하라. 여기서 c는 char 타입이다.
=============================================================================== 
menu item? 2
p1: seoul
p2: XYZ
+++++++++
X+p1: Xseoul
X+Y+p1: XYseoul
X+Y+Z+p1: XYZseoul
p1: seoul

/******************************************************************************
 * [문제 3] 
 ******************************************************************************/
// main() 함수 내의 3번 메뉴항목의 주석을 제거하라. 
// 아래 실행 결과처럼 출력될 수 있도록 필요한 연산자를 구현하라.
// name 멤버의 경우 (name += "<<") += c로 설정하라. 여기서 c는 char 타입이다.
=============================================================================== 
menu item? 3
p1: seoul
p2: XYZ
+++++++++
p1 << X: seoulX
p1 << X << Y: seoulXY
p1 << X << Y << Z: seoulXYZ
p1 << X << Y << Z: seoulXYZ


/******************************************************************************
 * [문제 4] 
 ******************************************************************************/
// main() 함수 내의 4번 메뉴항목의 주석을 제거하라. 
// 아래 실행 결과처럼 출력될 수 있도록 -- 연산자를 구현하라.
// 이 연산자는 이전의 p1 값을 보관하고
// address 멤버가 빈 문자열이 아닌 경우 address의 맨 마지막 문자를 제거한다.
// name 멤버의 경우 name+"--"로 설정한다.
// 보관된 이전 p1을 반환한다. 
=============================================================================== 
menu item? 4
p1: seoul
p2: XYZ
+++++++++
p1: seoul
p1--: seou
***
p1--: seou
p1----: seo

===============================================================================
== 전체 실행 결과
=============================================================================== 
menu item? 5
p1: seoul
p2: XYZ
+++++++++
name? P0         // 직접 입력하라.
address? SEOUL   // 직접 입력하라.
P0: SEOUL

menu item? 1
P0: SEOUL
p2: XYZ
+++++++++
P0: SEOUL
p2: XYZ

menu item? 2
P0: SEOUL
p2: XYZ
+++++++++
X+P0: XSEOUL
X+Y+P0: XYSEOUL
X+Y+Z+P0: XYZSEOUL
P0: SEOUL

menu item? 3
P0: SEOUL
p2: XYZ
+++++++++
P0 << X: SEOULX
P0 << X << Y: SEOULXY
P0 << X << Y << Z: SEOULXYZ
P0 << X << Y << Z: SEOULXYZ

menu item? 4
P0: SEOUL
p2: XYZ
+++++++++
P0: SEOUL
P0--: SEOU
***
P0--: SEOU
P0----: SEO

menu item? 0

Good bye!!

(끝)
```